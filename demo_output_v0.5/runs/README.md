# runs

Simulation and reconstruction outputs go here.

Typical files:

- `circle_obs.npz`: receiver traces, velocity model, coordinates, and world metadata
- `circle_recon.json`: grid-search reconstruction result

The `.gitignore` keeps generated run outputs out of version control by default.
