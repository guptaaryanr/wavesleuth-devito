# worlds

Generated world JSON files go here.

Example:

```bash
wavesleuth-devito generate-world --kind circle --out worlds/circle.json
```

The `.gitignore` keeps generated JSON files out of version control by default.
